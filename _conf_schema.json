{
  "enable_compact": {
    "type": "bool",
    "description": "启用 SoL-Astr",
    "hint": "开启后向 LLM 常挂 sol_astr_compact 工具，并在每轮用户消息尾部注入一行当前上下文 token 数。",
    "default": false
  },
  "dry_run": {
    "type": "bool",
    "description": "影子模式",
    "hint": "只记录工具触发台账，不调用摘要模型、不改写上下文。先用它观察触发率。",
    "default": true
  },
  "keep_recent_tokens": {
    "type": "int",
    "description": "保留最近多少 token（0 = 按模型窗口自动算）",
    "hint": "压缩时按整轮保留最近这么多 token。0 表示自动：取窗口的 15%，最多 40000。保留段越大压缩越不划算，因为压缩后首轮要按未命中价重建它。",
    "default": 0
  },
  "min_archive_tokens": {
    "type": "int",
    "description": "归档段至少多少 token 才压（0 = 按模型窗口自动算）",
    "hint": "0 表示自动：取窗口的 25%，最多 60000。它加上保留段就是起压门槛，会写在每轮的 context 提示里告诉模型。归档段必须远大于保留段才划算。",
    "default": 0
  },
  "strip_tool_trace": {
    "type": "bool",
    "description": "剥离压缩轮的工具调用记录",
    "hint": "压缩成功的那一轮，落盘前删掉 sol_astr_compact 的 tool_call/tool_result 对。",
    "default": true
  }
}
